- name: Add third-party feeds (移除失效镜像)
  run: |
    # 备份原始配置
    cp feeds.conf.default feeds.conf.default.bak
    
    # 1. 清理所有 passwall 定义
    sed -i '/^src-git passwall/d' feeds.conf.default
    # 2. 添加 passwall 源（直接使用 GitHub 原始地址）
    echo "src-git passwall https://github.com/xiaorouji/openwrt-passwall.git" >> feeds.conf.default
    
    # 3. 清理所有 rtp2httpd 定义（无则跳过）
    sed -i '/^src-git rtp2httpd/d' feeds.conf.default
    # 4. 添加 rtp2httpd 源（替换为真实地址，无则注释）
    # echo "src-git rtp2httpd https://github.com/xxx/rtp2httpd.git" >> feeds.conf.default
    
    # 5. 移除官方 feeds 的镜像（直接使用原始地址）
    sed -i 's#https://mirror.ghproxy.com/https://git.openwrt.org#https://git.openwrt.org#g' feeds.conf.default
    
    # 打印最终配置
    echo "=== Final feeds.conf.default ==="
    cat feeds.conf.default

- name: Update and install feeds (使用 GitHub 原始地址)
  run: |
    # 1. 强制清理旧 feeds
    ./scripts/feeds clean -a
    rm -rf ./feeds/* && mkdir -p ./feeds
    
    # 2. 尝试更新 feeds（使用 GitHub 原始地址）
    ./scripts/feeds update -a -f || {
      echo "⚠️ feeds update 失败，尝试手动克隆核心 feeds"
      # 手动克隆 passwall（使用 GitHub 原始地址）
      git clone --depth=1 https://github.com/xiaorouji/openwrt-passwall.git ./feeds/passwall || {
        echo "❌ 手动克隆 passwall 失败，尝试使用 fastgit 镜像"
        # 备用镜像：fastgit
        git clone --depth=1 https://hub.fastgit.xyz/xiaorouji/openwrt-passwall.git ./feeds/passwall || {
          echo "❌ 所有镜像均失败，请检查网络"
          exit 1
        }
      }
      # 手动克隆 rtp2httpd（替换为真实地址，无则注释）
      # git clone --depth=1 https://github.com/xxx/rtp2httpd.git ./feeds/rtp2httpd
    }
    
    # 3. 强制安装 feeds
    ./scripts/feeds install -a -f
    
    # 4. 终极验证目录
    echo "=== 终极目录验证 ==="
    if [ ! -d "./feeds/passwall" ]; then
      echo "❌ passwall 目录缺失，构建终止"
      exit 1
    else
      echo "✅ passwall 目录存在"
      ls -la ./feeds/passwall | head -10
    fi
